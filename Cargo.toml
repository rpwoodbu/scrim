[package]
name = "scrim"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
libc = "0.2"
chrono = "0.4"

[dev-dependencies]
tempfile = "3.8"
